# Following third party modules are required to run ResCon script which is written in Python 2.7:

# Biopython module. Written using version 1.63
Bio >= 1.63


# Optional modules but not required:

# natsort used for natural sorting. Used version 3.5.6
natsort >= 3.5.6


# Following are usually preinstalled by default in Python 2.7:
Tkinter
tkMessageBox
tkFileDialog
tkColorChooser



# Others tools required beside Python modules:
# Optionally Clustal Omega if it needs to be run locally. If not, it can be run using EMBL web server.